services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: oura
      POSTGRES_PASSWORD: oura
      POSTGRES_DB: oura_studios
    ports:
      - "5432:5432"
    volumes:
      - oura_pgdata:/var/lib/postgresql/data

volumes:
  oura_pgdata:
