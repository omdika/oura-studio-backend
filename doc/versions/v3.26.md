# v3.26 — Live Stok & Tautan Navigasi di Laporan Ranking Penjualan

**Status:** PLANNED
**Date:** 2026-08-26
**Changed by:** Design + Frontend + Backend
**Backend changes required:** Ya, memperbarui model `ProductSalesRankingEntry` di `app/schemas/reports.py` dan menambahkan kueri pergerakan stok live pada endpoint `/reports/sales-by-product` di `app/routers/reports.py`.

---

## B. Spesifikasi API & Perubahan Backend

### 1. Model Skema Pydantic (`app/schemas/reports.py`)
Tambahkan field baru `current_stock_qty` pada skema respons ranking:

```python
class ProductSalesRankingEntry(BaseModel):
    product_size_id: uuid.UUID
    product_name: str
    size_label: str
    fabric_variant_name: str | None
    qty_sold: int
    revenue: float
    profit: float
    current_stock_qty: int  # NEW: Live stock qty
```

### 2. Penambahan Kueri Live Stok di Endpoint Router (`app/routers/reports.py`)
Modifikasi kueri `/reports/sales-by-product` untuk menghitung sisa stok fisik saat ini dengan membaca `StockLedger` menggunakan helper `_stock_qty_map` (sebagaimana pola yang ada di router products):

```python
    # ... kueri ranking teragregasi yang sudah ada ...
    
    size_ids = [r.product_size_id for r in results]
    from app.routers.products import _stock_qty_map
    stock_map = _stock_qty_map(db, size_ids)

    return [
        ProductSalesRankingEntry(
            product_size_id=r.product_size_id,
            product_name=r.product_name,
            size_label=r.size_label,
            fabric_variant_name=r.fabric_variant_name,
            qty_sold=int(r.qty_sold),
            revenue=float(r.revenue),
            profit=float(r.profit),
            current_stock_qty=stock_map.get(r.product_size_id, 0), # NEW
        )
        for r in results
    ]
```
