# v3.28 — Redesain Kasir Terpuncak & Perbaikan Peringatan Stok Kosong

**Status:** PLANNED
**Date:** 2026-08-26
**Changed by:** Design + Frontend + Backend
**Backend changes required:** Ya, memperbarui Pydantic model `LowStockAlert` di `app/schemas/reports.py` dan memperbaiki kueri kustom di `app/routers/reports.py`.

---

## B. Diagnosis & Perbaikan Bug Peringatan Stok (Fullstack)

### 1. Analisis Bug: Mengapa Stok Kosong (0 pcs) Tidak Muncul?
- Endpoint `/reports/dashboard` memanggil `_low_stock_alerts(db)`. Pada kode lama, kueri database menyertakan filter `.filter(ProductSize.reorder_min_qty.isnot(None))`.
- Ketika varian ukuran baru ditambahkan (seperti XS, S, M bernilai `0 pcs` dan berstatus "Belum ada harga"), field `reorder_min_qty` di database secara default bernilai `null` (`None`).
- Akibatnya, semua varian berstok kosong tersebut ter-filter keluar oleh kueri ORM lama dan tidak pernah muncul di dashboard.

### 2. Perbaikan Sisi Backend
- **Pembaruan Skema Pydantic (`app/schemas/reports.py`):**
  Ubah skema `LowStockAlert` agar `reorder_min_qty` bersifat opsional: `reorder_min_qty: float | None = None`.
- **Optimasi & Perbaikan Kueri (`app/routers/reports.py`):**
  - Hapus filter `reorder_min_qty.isnot(None)`.
  - Gunakan `_stock_qty_map` bulk query untuk mengoptimalkan performa.
  - Logika pemicu alert:
    `is_empty = (current_stock == 0)`
    `is_low = (min_qty is not None and current_stock < min_qty)`
    Jika `is_empty` atau `is_low` terpenuhi, item dimasukkan ke daftar alert.
