# v3.22 — Ranking Penjualan per Produk

**Status:** DONE
**Date:** 2026-08-25
**Changed by:** Design + Frontend + Backend
**Backend changes required:** Ya, menambahkan endpoint baru `GET /reports/sales-by-product` pada router `reports.py` di backend.

---

## B. Spesifikasi API & Perubahan Backend

Aplikasi iOS telah memodelkan `SalesByProductItem` dan memanggil `/reports/sales-by-product` pada `APIService.swift` di frontend.

### 1. Model Skema Pydantic (`app/schemas/reports.py`)
Tambahkan skema respons baru:

```python
class ProductSalesRankingEntry(BaseModel):
    product_size_id: uuid.UUID
    product_name: str
    size_label: str
    fabric_variant_name: str | None
    qty_sold: int
    revenue: float
    profit: float
```

### 2. Penambahan Router Endpoint (`app/routers/reports.py`)
Tambahkan endpoint baru di bawah `/reports` prefix:

```python
@router.get("/sales-by-product", response_model=list[ProductSalesRankingEntry])
def product_sales_ranking(
    from_: date | None = Query(default=None, alias="from"),
    to: date | None = Query(default=None),
    db: Session = Depends(get_db),
):
    # ... kueri gabungan SQLAlchemy teroptimasi
```
